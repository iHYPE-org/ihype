import { PlatformFrame } from '@/components/fan-app/PlatformFrame';

export const metadata = {
  title: 'iHYPE — Fan App',
  description: 'Listen, hype, get tickets. Direct to artist. No fees.',
};

export default function AppPage() {
  return <PlatformFrame />;
}
