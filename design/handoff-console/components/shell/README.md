Drop-in replacement for the design system's `components/shell/`.

14 components, re-anchored to the tokens. `RotaryNav`, `TunerDial` and
`JoystickTransport` are rebuilt as real instrument hardware AND reconciled to
the shipped dock geometry — both knobs default to 74px, the dial to the bar's
height. Prop signatures unchanged; keep the existing `.d.ts` files.
