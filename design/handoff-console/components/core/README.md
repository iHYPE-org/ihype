Drop-in replacement for the design system's `components/core/`.

23 components, re-anchored to the tokens. Prop signatures unchanged — the
existing `.d.ts` and `.prompt.md` files beside them stay valid and must NOT be
deleted. Replace the `.jsx` only.
