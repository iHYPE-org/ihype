<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links: opens ihype.org links directly in the app instead of
                 the browser, once /.well-known/assetlinks.json (see the Next.js
                 route of the same path) is verified against the real signing
                 cert — inert but harmless until then. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="ihype.org" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ will not show the push prompt unless this is declared. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- FINE beside COARSE, and the map does not work without it below API 31.

         Capacitor's BridgeWebChromeClient.onGeolocationPermissionsShowPrompt
         requests ACCESS_FINE_LOCATION and ACCESS_COARSE_LOCATION together and
         ANDs every result in one shared callback, so an undeclared FINE is an
         automatic denial that fails the whole request. Its coarse-only rescue
         path is gated on SDK_INT >= 31, and minSdkVersion here is 24 — so on
         Android 7 through 11 the map silently never started where the member
         is, even after they tapped Allow. /app/map is WORKBENCH_PATH, the
         surface the app opens on.

         Declaring FINE does not force precise location on anyone: Android 12+
         still shows the Precise/Approximate choice in the OS dialog, and the
         member can pick Approximate. What it removes is the case where they
         are given no working option at all. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
</manifest>
